# Mapping Workflow Review and Implementation

Review: 2026-09-13 America/Denver; verification completed on 2026-09-14 UTC.
Baseline: `eda1ada05c03ef2244b8bbc4e3b6fb8380051389`, `main`.
Status: first safety/workflow packet implemented; acceptance results below. The full Design-to-Layout program is not complete.

Continuation: [full-refactor-execution.md](full-refactor-execution.md) tracks the active next pass, including dependency remediation, CRS qualification and the newly specified hardware. The preceding packet and its evidence remain historical, immutable acceptance scope.

## Scope and Decision Record

Complexity band: xhigh. Selected coordinator reasoning effort: xhigh, because geometry feasibility, asynchronous persistence status, renderer behavior and advisory governance cross module boundaries. Subagent decision: not useful for this serialized repair/validation packet. The coordinator is the sole writer; no independent panel approval or weighted vote is claimed. Future independent review must name reviewers, evidence, disagreement and actual votes rather than treating generated role weights as consensus.

The current checkout, not historical plan completion statements, is the implementation authority. Preflight was clean. Current changes preserve projected/local XY, project schema v1, existing archive/storage contracts, native proof gates and the no-cost/offline-first policy. No dependencies, native configuration, receiver commands or relay wiring are changed.

Chosen order: numerical and truthfulness regressions first; then draft/catalog transactions and CRS qualification; then immutable Layout handoff and model optimization. The fixed workspace remains the primary interface, with optional guided steps. Do not replace it with a mandatory wizard or a landing page.

## Implemented Packet

| Area | Change | Limit |
| --- | --- | --- |
| Geometry area | Origin-translated, compensated shoelace accumulation reduces cancellation at large projected offsets. | Floating-point geometry, not a survey accuracy guarantee. |
| Coverage containment | Circumscribed arcs use a 0.001 m approximation policy and a bounded segment budget for wet-coverage checks. Inscribed display polygons no longer establish containment by equality. | Tolerance is a calculation policy, not field error or continuous corner-arm clearance. |
| Mechanical screening | Hard obstacles intersect the whole base-machine sweep, including partial-sweep end buffers and obstacle buffers, not only tower/end tracks. | Conservatively flags potential conflicts; no assumption that a span can pass above an object. Tower-track diagnostics remain separately reported. |
| Scoring | Explicit radius, coverage, outside-area, obstacle and hard mechanical limits disqualify alternatives; invalid weights/constraints are rejected. Optimizer-derived scenarios use the same normalized scoring entrypoint. | Not yet a universal feasibility engine or a Pareto optimizer across every advisory model. |
| Render/advisory model | Explicit zero end-gun throw is preserved. Public fallback corner equipment is opt-in. Selected end-gun sectors survive rendering. Corner preview uses the supplied configuration and subtracts no-spray polygons. | Derived outlines and public metadata do not establish a field-specific equipment profile. |
| Corner kinematics | SDU samples use arm-length circle/polyline intersections; unreachable guidance blocks output. Rotation affects sampling, and full-circle sampling includes its closing position. | Nearest previous intersection is only a sampled branch heuristic. Results without sample failures are `unresolved`, not field-ready. Continuous branch, speed, steering and swept clearance remain unproved. |
| Save state | Samples/new designs begin unsaved. Saves are serialized; acknowledgments are tied to the loaded project generation and captured revision. | Does not introduce atomic catalog creation, autosave, or persisted incomplete drafts. |
| Manual design | The transaction badge says `Can apply`, not `Ready`. | Reducer eligibility is not RTK, hydraulic or field qualification. |
| KML selection | Counts and replacement warnings recalculate from selected items; stale-project and repeated-apply guards retain the preview on errors. | No parser/schema or Google Earth render-proof change. BPF selection parity is still follow-up work. |
| Help/accessibility | Wizard step review and all seven checklist items are independent. Clipboard success requires fulfilled write; failure is visible and example text selectable. Checkbox ARIA states are exposed. Shared Help adds save/RTK/kinematic limits. | Native screen-reader, clipboard and touch behavior still require device evidence. The end-gun sample's misleading factory-input description is also corrected, but that field is not persisted or displayed by the current sample factory. |
| Governance | Shell denials inspect known shell command payloads, not arbitrary patch/MCP text. Explicit no-delegation wording overrides advisory keyword matching. Context hashes cover config, guard/disabled Stop scripts and skill references. Panel weights reject nonfinite, negative, boolean and duplicate-role inputs. | Checkout-controlled data and hooks remain advisory. This is neither a complete shell parser nor deployed managed policy enforcement. |

Changed ownership: `packages/geometry/src/`, geometry test registration, the end-gun sample description in `packages/core/src/sampleProject.ts`, `apps/mobile/App.tsx`, `apps/mobile/src/components/GoogleEarthImportWizard.tsx`, `ProjectFilesPanel.tsx`, browser regressions, `.codex/hooks/`, `tools/build_cplayout_context_map.py`, hook tests and linked documentation. No project-store or core document contract mutation is part of this packet.

## Verification Record

- Six new workflow/geometry counterexamples failed on the original implementation and passed after correction. These cover area translation, between-track hard obstacles, inscribed-circle false containment, ignored hard limits, invalid weights and silent fallback equipment.
- Focused geometry, scoring, advisory placement, render-model and kinematic checks passed during development. Preview precedence initially failed when a saved corner configuration overrode the supplied preview; evaluation now uses the supplied configuration. New no-spray and rotation/closure regressions are included in the aggregate command.
- Full-cycle corner regressions exposed a polygon-union failure at the span/overhang joint. One collinear physical member and a normalized closing trigonometric angle remove that numerical joint. Both directions are tested at local and large projected offsets. Fine 10-degree sampling closes with `unresolved`; coarse 30-degree nearest-intersection sampling changes branch and retains an explicit angle blocker. The latter is a known heuristic limitation, not a field-feasible path. An initial assertion expecting all sampling resolutions to be unresolved failed and was replaced with reason-specific tests for both outcomes.
- Aggregate testing found a stale end-gun sample assertion: zero spray overlap does not mean zero mechanical conflicts. The nearest shed corner is outside the 198.5 m physical radius but inside radius plus 8 m machine and 12 m shed buffers. The unchanged geometry now expects the specific `building-pad` / `machine_path` warning; its misleading description was corrected. The focused sample and final visual-layout tests pass.
- Two frozen scheduling outputs changed only in area-derived numbers. A separate calculation against the pre-change validated source reproduced all nine original hashes before comparison. Candidate order, positions, identifiers, warnings and selected machines did not change in that comparison.
- Initial browser diagnostic: six passed, three failed, six not run after the failure cap. It found missing web checkbox state plus an ambiguous test locator. These failures are retained, not relabeled as passes.
- Verification environment correction: the first Linux copy omitted `packages/core/node_modules`, incorrectly resolving root XML 0.8.15 instead of core XML 0.9.12. Root XML tests passed; the incomplete copy failed a GPS-only XML round trip. Restoring the existing nested dependencies corrected resolution without changing package versions or the lockfile. Earlier 15- and 24-case browser reports are diagnostic only, not final acceptance.
- Pre-caption corrected-dependency export: 24 workflow/mapping/scheduling browser cases passed with zero retries across desktop, 768-pixel tablet and 390-pixel mobile. A broader run passed 19 cases (all 14 selected desktop cases and five tablet cases) before intentional interruption for the caption rebuild; one was interrupted and 22 did not run. It is not a full-suite pass. Desktop full-cost and partial-sweep report cases took about 2.2 and 1.8 minutes; no speed improvement or baseline comparison is claimed.
- Sample persistence, checklist independence, rejected clipboard writes, selection-warning refresh and cancellation are covered. Save acknowledgment races and stale/repeated import guards still need dedicated injected-failure browser tests; code inspection alone is not that evidence.
- Root audit refreshed: five high, zero critical, all in the Expo/Metro/image-size chain. No forced fix, override or package installation was applied.
- Full `npm run validate` passed in the source-matched Linux copy after the corrections, including all workspace typechecks and tests. Workspace governance validation and context-map freshness passed; `git diff --check` passed.
- Final browser selection: 87 passed, zero failed, skipped or flaky, zero retries, across desktop, 768-pixel tablet and 390-pixel mobile (29 cases per viewport). Coverage includes manual Design staging/apply, read-only Layout mode, SVG fallback, Help/RTK navigation, saves/imports, offline MapLibre pixels and pan, delayed-imagery draft retention, attribution sanitization, simulated serial lifecycle and ZIP evidence preservation. Desktop and mobile map screenshots plus desktop checklist screenshots were visually inspected. This selected set is not the complete browser suite or a release pass. Full-cost and partial-sweep export cases passed on desktop before the description-only rebuild; final-build tablet/mobile performance for those cases remains untested.

Frozen-output audit (SHA-256):

| Fixture/calculation | Previous | Updated | Reviewed difference |
| --- | --- | --- | --- |
| Baseline field plan | `451b5f9799768b00eed42d0154d7e2c676cfe1eda1706be952418fb6f9e26324` | `f96a75183843732a26f45bb4e30279afabb8bfa8bd24173c970e568d62b91c93` | Nine area/percentage fields, maximum displayed change 0.000001 acre or percentage point. |
| Will Rhea multi-machine review | `3f61d2b077759e41b87a16551bb6b172a0b2038f3363c7350bc25f966b8efe0c` | `ca8e7ab0a4d32759389814f562e2739c4ce8877761a678033dba2c810444c586` | Four zone-acre fields; largest absolute change below 0.0000004 acre. |

The other seven hashes are unchanged. The rendering scheduling fixture now explicitly requests its historical public fallback equipment; the application does not. This retains its long-envelope yielding assertion without reinstalling fallback equipment in the product.

Validation commands:

```sh
npm run validate
npm run context-map:build
npm run validate:skills
git diff --check
npm audit
npm run ui:test:start -- --no-open --port 19008
CPLAYOUT_WEB_PROOF_PORT=19008 npm run test:web:e2e -- \
  tests/web/workflow-safety.spec.ts tests/web/mapping-quality.spec.ts \
  tests/web/advisory-performance.spec.ts tests/web/maplibre-runtime.spec.ts \
  tests/web/rtk-lifecycle.spec.ts tests/web/browser-workflow.spec.ts \
  --grep 'workflow-safety|mapping-quality|advisory-performance|maplibre-runtime|rtk-lifecycle|SVG map fallback|file menu opens curated|catalog blank design|public proof map features|help training|guided manual design|layout mode keeps|Will Rhea guided|google earth import wizard|files projected geojson import can be saved|files survey csv import can be saved|recent-project row' \
  --workers=1 --retries=0 --reporter=list,json,html --output=accepted-browser-results
```

Browser evidence uses an audited Linux-local source/dependency copy at `/home/cyber/cplayout-workflow-20260913-K6di9e/`, served by the repo launcher at `http://127.0.0.1:19008`. It leaves the earlier root preview on 19006 untouched. Installed package contents are not individually hashed; dependency equivalence checks cover installed package manifests. The final exported JavaScript is `index-6285a8b55e0a4dc992b4d1e2a605d194.js`. The preceding `index-809b615ead4b89d65b3ca70f985346e5.js` differs in application source only by the corrected sample factory description; the sample test also changed. The earlier identity manifest records that boundary.

Final browser reports use `accepted-browser-*`; source/export identity is recorded in `accepted-identity.json`. At 2026-09-14 04:52 UTC, 306 source/config files and 693 installed package manifests matched between workspace and verification copy, with ten exported assets hashed and zero mismatches. Identity manifest SHA-256: `df25941e375038c18dc57ea1b52de7848bffc97e599f069341b30a654ee858f2`.

Older `qualified-final-workflow-*`, `qualified-final-routes-*`, `workflow-v2-*`, `workflow-final-*` and `diagnostic-v1-*` browser reports are diagnostics or superseded-build results despite their names. Failed aggregate logs are retained as `diagnostic-source-validation-missing-nested-xml.log`, `diagnostic-source-validation-coarse-branch.log` and `diagnostic-source-validation-sample-clearance.log`. The passing aggregate log is `qualified-final-source-validation.log` (SHA-256 `97c5c0e67cc6dd75704bc8a9ad401a20010124d7e73abc687fb781fbb69f8803`). Any later application source or asset change invalidates export acceptance until rebuilt and checked again; test-only changes require rerunning their affected tests. Native hardware, installed managed hooks and actual Google Earth are not exercised by these commands.

The final identity, result JSON, validation/audit logs and selected screenshots are retained locally under `reports/continuous-improvement/mapping-workflow-20260914/`; complete browser reports remain in the Linux verification copy. These ignored artifacts are local evidence, not published release artifacts. The launcher-owned 19008 preview is intentionally left running for user review; cleanup was not requested. Stop it from the verification-copy directory with `npm run ui:test:stop`. Existing 19006/19007 previews are not this build and were not modified. No commit, push, branch change or new migration was performed in this implementation packet.

## Resume Checkpoint

1. Re-read this record and `AGENTS.md`, inspect the dirty tree and preserve all audited/unrelated work. Do not repeat the historical GitHub transfer or treat the existing baseline commit as containing this packet.
2. Start the next implementation packet with incomplete-draft/catalog contracts and CRS qualification; settle their migration/read-compatibility tests before immutable Layout snapshots. Use the module ownership and gates below. Do not combine these changes with dependency or native receiver upgrades.
3. For another Linux verification copy, include all workspace-local dependency directories, not only root `node_modules`. Compare source, lockfile and resolved package manifests before exporting. Freeze application source for each browser batch; keep diagnostics separate from acceptance and record any intentional interruption.
4. Run cheap focused counterexamples before the aggregate command. After a source freeze, run one aggregate source pass, governance validation when relevant, and one exact-build browser selection; broaden to the complete browser/device inventories before release. Reuse verified dependencies only while their manifests and lockfile remain unchanged.
5. Before publication, investigate current branches/remotes and stage audited paths only. Keep native, hydraulic, continuous-mechanical and 3D-field claims blocked until their own evidence gates pass.

## Remaining Implementation Sequence

Each packet needs a failing example, a bounded patch, reviewed diff, targeted tests, aggregate validation and exact-build browser/device evidence where relevant. Do not combine storage migration with native GNSS or renderer upgrades.

1. **Draft/catalog foundation.** Introduce an explicitly incomplete design outside `PivotProject` v1. Remove the fabricated rectangle/pivot/machine from blank creation without making invalid legacy project documents unreadable. Create catalog design plus draft in one repository transaction; web and native must share failure/rollback semantics. Files: core draft types/validation, project-store web/native repositories, archive handling and App/catalog actions. Test each missing input, duplicate create, failed save, reload recovery and schema migration rollback.
2. **CRS qualification.** Keep old XY documents readable without rescaling. Add a calculation/field-qualification policy separate from the legacy CRS parser; reject unverified units, Web Mercator ground-distance use and unsupported datum transformations for field qualification. The current broad NAD27/NAD83 ranges admit non-UTM codes, including foot-based `EPSG:26741`. Files: core units/coordinates/project document, geometry entrypoints and GNSS admission. Test supported areas, axes, units, grid-to-ground scale and legacy read/repair/export preservation against independent PROJ fixtures. No guessing an EPSG definition from its last two digits.
3. **Design-to-Layout handoff.** A committed design produces an immutable versioned Layout snapshot, target IDs and provenance; observations are append-only and reference that exact snapshot. Changing Design invalidates or explicitly supersedes a Layout session, never silently moves its targets. Add archive v2 alongside legacy project v1 with backup/downgrade refusal and round-trip tests. Files: core contracts/reducers, both project repositories, projectArchive, map adapters and survey/navigation surfaces.
4. **Unified feasibility and optimization.** Share a `feasible/blocked/unresolved` result with reason codes and source/tolerance provenance across direct, scenario, optimizer, manual and advisory paths before scoring. Candidate rankings must be comparable; introduce deterministic Pareto alternatives only after hard constraints pass. Test concave fields, tiny slivers, tangencies, holes, duplicates, rotated/transformed inputs and all-zero weights. Keep unknowns ineligible, not low-scoring eligible designs.
5. **Equipment models.** Add source-confirmed machine profiles and physical-vs-wetted envelopes. Corner solvers need branch enumeration, continuity, asymmetric steering, both travel directions and bounded continuous clearance. Linear/lateral models need independent span width, travel path, feed constraints and swept union, not reuse of pivot reach. Field-specific dimensions and manufacturer inputs remain required. Add an independent geometry oracle and regression corpus before using model output for installation.
6. **Hydraulic/agronomic review.** Keep geometric acreage distinct from water application. Add measured catch-can data, distance/area weighting, flow/pressure/nozzle/speed inputs, loss/intensity checks and explicit missing-input states. Validate formulas with independently computed fixtures and source versions. No invented uniformity or end-gun reach defaults, no certified irrigation recommendation from imagery alone.
7. **Usability and help parity.** Preserve stable workspace navigation; make mode, active design/version, dirty state, selected layer and blocked action reason visible. Consolidate a capability/help registry used by web/native, menus and export notes. Cover undo/retry/cancel, keyboard and touch alternatives, import-selection parity, target loss, stale snapshots and interrupted saves. Expand accessibility checks beyond the two corrected components; do not assume existing `accessibilityState` props reach web ARIA.
8. **Performance.** Bind every calculation to immutable inputs and model version; invalidate stale outputs. Benchmark projection, clipping, candidate generation, reports and browser interactions separately with the same source/export and fixed fixtures. Scheduling yields do not bound uninterrupted calculation time. Optimize measured hot paths and cache repeated obstacles/envelopes only with semantic-equivalence tests; no fresh scoring or geometry changes mixed into baseline performance comparisons.
9. **RTK and relay qualification.** Android first; PX1122R and NS-RAW are separate processing/transport lanes. Preserve USB/Bluetooth/local-Wi-Fi selection, but do not assign ESP32 relays without inspected EVB revision, electrical routing, contact/polarity and command protocol. Model break-before-make, acknowledgment, timeout, disconnect and recovery without energizing unknown hardware. For acceptance use independent controls, antenna/reference heights, common 3D comparison frame, uncertainty and time provenance. Maximum observed 3D error must be strictly below 0.10 m; equality fails. Later iOS needs separate compatibility/device evidence.
10. **Dependency and release packets.** Resume the recorded Expo-compatible security packet; do not retry the rejected blanket Metro override without resolving its API mismatch. Review licenses, offline behavior, keys/cloud dependence and native rebuild impact before installing anything. Reconcile full browser inventory and source/asset hashes, then native SQLite/ZIP/MapLibre and physical RTK evidence. Confirm Git hosted state before any new publication; the prior transfer and single-main consolidation are historical completed actions, not work to repeat.

## Primary Evidence and Unverified Claims

- [PROJ Web Mercator](https://proj.org/en/stable/operations/projections/webmerc.html), refreshed 2026-09-13 local: projection formulas/domain support separating web display from ground-distance qualification. Local `projinfo EPSG:26741 -o PROJ` identified US survey feet; legacy admission remains a code-review finding, not repaired by this packet.
- [UGA Circular 911](https://fieldreport.caes.uga.edu/publications/C911/evaluating-and-interpreting-application-uniformity-of-center-pivot-irrigation-systems/), refreshed 2026-09-13 local, minor revision June 26, 2026: measured catch-cup distance/volume and pivot-specific uniformity support a separate measured application workflow. This packet does not implement or claim compliance with ANSI/ASAE S436.2.
- [OpenAI hook coverage](https://developers.openai.com/codex/hooks#tool-coverage), refreshed 2026-09-13 local, plus installed scripts/types: tool/event coverage and managed loading are distinct from the trustworthiness of mutable checkout inputs. Tests prove local payload handling only.
- Installed React Native accessibility declarations and React Native Web `createDOMProps` confirm the new ARIA props; browser regression is the behavioral evidence. Native parity remains untested.
- [Dependency decision record](dependency-upgrade-plan.md), [GNSS plan](rtk-gnss-integration-plan.md), [native checklist](android-native-verification.md) and [prior quality evidence](mapping-rtk-quality-program.md) remain authoritative for their dated, bounded claims.

Not demonstrated: best-in-class ranking, production irrigation design, actual less-than-10-cm 3D field accuracy, native transports/relay switching, native map or storage runtime, raw native PMTiles/MBTiles, Google Earth visual fidelity, deployed hook enforcement, or completed immutable Layout handoff.
