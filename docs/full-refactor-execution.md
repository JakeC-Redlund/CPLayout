# Full Refactor Execution

Started 2026-09-13 America/Denver; continuing on 2026-09-14 UTC.
Status: active, incomplete. This record resumes [the mapping workflow review](mapping-workflow-review.md); it does not replace or reduce its remaining scope.

## Relocation Checkpoint: 2026-09-14

The owner requested an immediate commit, push and sync for office relocation. Development acceptance is `interrupted_by_user_not_a_validation_pass`. This is a source-preservation checkpoint, not a release. Complexity: high; selected reasoning: high; Subagent decision: not useful for this publication pass. Preserve the full remaining goal on resumption.

- Live Git preflight confirmed public `JakeC-Redlund/CPLayout`, only local/remote `main`, and no divergence after fetch. All audited tracked/nonignored work, including unfinished archive source, is included in the checkpoint; raw private runtime artifacts and dependency caches are not published.
- Pre-publication rollback material is retained outside the relocating workspace mount in `/home/cyber/cplayout-relocation-20260914-1gqJZs/`. The record includes the original commit, worktree paths, source hashes and dirty diff. Publication verification is separate from these pre-publication artifacts.
- Fresh audit: zero findings. Skill/governance validation passed. The last corrected geometry-focused run passed 66 cases; 51 selected browser cases passed on the earlier `F8dZtG` candidate, whose aggregate validation remains failed. Full corrected-candidate validation and complete browser proof have not run.
- Draft archive integrity repair has partial worker changes and 19 original tests still passing, but no independent adversarial re-review. It remains unexported, unregistered and unaccepted. Finish CRC, UTF-8, header/descriptor negative cases before integration. Worker/reviewer sessions reached their runtime usage limit; no new workspace storage-contract files were created.
- The prepared `/home/cyber/cplayout-corrected-20260914-IT5Act/` snapshot has 518 matched paths, exact clean installation, installed-tree review and zero full/production audit findings. It excludes the two unaccepted archive files and has no aggregate or browser result. Do not report it as a validated application.
- Owned preview servers on ports 19011 and 19012 were stopped through the repository launcher, with no owned server remaining in either snapshot's status. Earlier unrelated listeners were not targeted. The native emulator and Metro had already been stopped.

Resume with current `AGENTS.md`, Git status/remotes/ancestry and this acceptance queue. Confirm the published checkpoint before edits; re-establish available reviewer tools without inventing independent approval. First complete the clearance correction review and archive adversarial repair, then freeze the actual combined source, run `npm run validate`, `npm audit`, `npm run validate:skills`, `git diff --check`, and complete `npm run proof:web` with an explicit unused proof port. Continue atomic draft/catalog storage, draft-aware UI and the remaining Design-to-Layout packets. Native restart/migration/ZIP/MapLibre and physical receiver/electrical/less-than-0.10-m 3D qualification remain open.

## Goal and Evidence Policy

Finish the Design-to-Layout refactor, address the five remaining high dependency findings, and establish current native and physical 3D evidence for the owner-selected hardware. The owner requires maximum measured 3D error strictly below 0.10 m, including elevation. See [receiver hardware qualification](receiver-hardware-qualification.md) for the declared PX1122R Version 1, NS-RAW, ESP32 DevKitC V4, peer-to-peer S3B and SunFounder four-channel 5 V assembly.

No synthetic observation, receiver precision estimate, firmware acknowledgment, report-validator pass, web screenshot, or historic native build can satisfy the physical requirement. No comparative best-in-class claim is made without an independently defined and executed benchmark.

Complexity band: xhigh. Selected coordinator reasoning effort: xhigh for cross-module coordinate safety, dependencies, native proof and electrical integration. Subagent decision: required. The coordinator owns integration and remains on the critical path. Independent scopes in this pass:

- Hardware specialist, xhigh: primary-source electrical/receiver research, then only a pure transport-switch controller and its fake-clock tests. No wiring, firmware, relay IO, package or document writes.
- CRS worker, xhigh: coordinate transformation/qualification helpers and focused core tests only; no schema, geometry, UI or package changes.
- Runtime reviewer, high: read-only host/device/build-evidence audit and emulator prerequisites; no hardware configuration or app-data mutation during enumeration.
- QA reviewer, high for the dependency diff, xhigh for subsequent cross-module CRS admission review: independent read-only findings and acceptance boundaries.

Evidence-backed safety vetoes take precedence over preference scores. Reviewer conclusions must name their actual evidence; these assignments are not a claim that a weighted panel vote, hardware test or managed policy deployment occurred.

## Worktree and Checkpoints

Baseline HEAD is `eda1ada05c03ef2244b8bbc4e3b6fb8380051389` on `main`. This pass began with 37 modified/untracked paths from the preceding implementation. They are preserved. No new transfer, branch consolidation, destructive reset or force push is needed to repeat historical Git work.

The preceding source/browser checkpoint is documented in the workflow review and locally retained under `reports/continuous-improvement/mapping-workflow-20260914/`. Its 87 selected browser passes are not a complete suite or acceptance for later source changes.

The dependency experiment is isolated at `/home/cyber/cplayout-metro-security-20260914-K1O2Fp/`, with exact before-state lock/manifest and structured evidence. The previous accepted preview/copy at `/home/cyber/cplayout-workflow-20260913-K6di9e/` is not overwritten. Preview ports 19006-19008 are historical builds and remain untouched by the new candidate.

## Acceptance Queue

| Packet | Current disposition | Required closure |
| --- | --- | --- |
| Expo/Metro security | Reviewed coherent patch installed; root/production candidate audits have zero findings, installed/config tests pass, 18 selected dependency-browser cases pass. | Integrated full source/browser checks remain separate from the accepted dependency-only baseline. See the exact packet and rollback in the dependency record. |
| CRS transformation and qualification | Exact UTM helper, guarded geometry/reducer/GNSS admission and legacy recovery UI implemented; independent KML/save/open findings corrected. Twelve recovery browser cases pass across three viewports. | Complete integrated and full browser regressions. A metre grid still does not establish area-of-use, grid-to-ground scale, datum/epoch, elevation or field accuracy. |
| Transport/relay safety | Pure fail-closed controller exported and registered; 24 synthetic tests passed and independent controller review found no concrete defect. | Serialized IO adapter integration, a physically verified electrical route contract, then actual USB/Bluetooth/Wi-Fi/native and contact behavior. No route is enabled from the owner inventory alone. |
| Native baseline | Resource repair, bounded CMake build and installed APK identity pass. The isolated emulator observed schema-v11 SQLite CRUD/settings/delete and an in-memory ZIP round trip. Overall collector failed at OS UI navigation; ANR/onboarding and collector defects are recorded below. | Isolated two-process schema-v11 save/relaunch/migration/ZIP, received-file and consumed-bundle identity, and current MapLibre/gesture evidence. Emulator evidence cannot prove receiver hardware. |
| Incomplete drafts and catalog | Core document foundation exported/test-registered after 27 focused tests, independent review and combined source validation. Existing blank creation still fabricates a complete project; read-only transaction review completed. | [The draft/catalog packet](draft-catalog-implementation.md): atomic create/save/recovery, migration, archive preservation and draft-aware UI. |
| Immutable Layout handoff | Open. | Versioned target snapshot, append-only referenced observations, stale-design/session invalidation and compatible archive v2. |
| Unified feasibility and optimization | Prior hard-limit/scoring repairs implemented; universal result/Pareto handling open. | Feasible/blocked/unresolved semantics at every entrypoint, hard constraints before ranking, independent geometry corpus. |
| Equipment and hydraulics | Sampled corner output remains unresolved, not field-qualified. | Source-confirmed dimensions, continuous branch/clearance and independent linear models; measured water application and uncertainty. |
| Interface/help/accessibility | Prior checklist/save/import fixes implemented; complete parity open. | Recovery, retry, stale results, target/version state, all-interface help/capability registry and native accessibility evidence. |
| Performance and release | Open. | Immutable job inputs, measured hot-path benchmarks, complete browser inventory, native/field proof and reviewed publication. |

The detailed module boundaries and negative test cases for these packets remain in [Remaining Implementation Sequence](mapping-workflow-review.md#remaining-implementation-sequence). Hardware absence does not stop unrelated software work. Conversely, software progress does not close native/field gates.

## Validation and Resume

1. Re-read `AGENTS.md`, this queue and actual diffs. Preserve all ongoing independent work; do not infer ownership from file timestamps.
2. Integrate only completed, reviewed worker scopes. Run focused negative tests before aggregate tests; record rejected experiments as rejected.
3. For dependency changes, compare every lock entry and manifest against the packet before-state. Review exact registry integrity, license, engine and lifecycle metadata before installing. Run `npm ci --ignore-scripts`, the separately reviewed existing UUID helper, `npm ls --all`, root and production `npm audit`. Never use a forced audit repair.
4. Run `npm run validate` on source- and dependency-matched files. Copies must include workspace-local nested dependencies. Do not change a running verification copy's source or installed graph; freeze and hash it first.
5. Use the repository launcher and exact printed URL for browser checks. Run `npm run proof:web` for complete browser acceptance; selected tests are reported as selected. Preserve source/asset hashes, failures, screenshots, skips and cleanup state. Rebuild after application changes.
6. Run `npm run validate:skills`, `npm run context-map:check` and `git diff --check` for affected governance/documentation. Generated maps remain advisory.
7. Perform native/hardware work only through identified targets and scoped tools. Preserve user data, existing emulators/listeners, keystores and rollback artifacts. Record missing observations without synthesizing evidence.
8. Resume the next open queue item after each accepted packet. Keep the overall goal incomplete until required work and evidence are genuinely complete; record physical blockers separately from remaining executable software work.

No commit or push has been performed in this pass. Publication requires fresh remote/branch/ancestry checks and staging audited paths only. A clean source tree cannot substitute for the still-required native and 3D field evidence.

## Integration Notes: 2026-09-14

The combined verification copy is `/home/cyber/cplayout-refactor-20260914-F776BB/`. An initial directory copy omitted nested package `dist` files, so its first tests failed to find tsx/proj4 modules. This was a preparation failure, not product evidence. A fresh `npm ci --ignore-scripts --audit=false`, followed by the existing UUID helper, rebuilt the exact graph including workspace-local dependencies. Later synchronization uses Git's tracked/nonignored file list; no running copy is changed while its validator/export is active.

The first integrated validator rejected a historical geometry-test message expectation after the deliberate admission error changed. That expectation is corrected; seven synthetic positive metre fixtures (including provenance) now explicitly use metre-grid arithmetic rather than pretending an undeclared LOCAL label establishes units. New tests separately require LOCAL, feet, unknown EPSG and Web Mercator rejection while preserving stored XY, undo and recovery. The 31 focused core/geometry/GNSS cases passed; that result is not a replacement for the repeated full validator.

Independent review identified and drove fixes for KML line-length/circle expansion bypasses and raw circle-center reuse across destination grids. Empty circle metadata is also rejected instead of being coerced to zero. KML metric regressions pass. A second independent review found catalog reassignment after a Files ZIP import and stale asynchronous project opens. Independent loads now clear catalog affiliation, and a latest-request guard rejects stale completions. Six request-guard tests pass. No known review finding is dismissed by a passing unrelated test.

The frozen `F776BB` copy passed `npm run validate` and web export, but predates the final empty-circle-metadata regression. The final browser copy is `/home/cyber/cplayout-web-final-20260914-uoDSm5/`: 317 application/tool/test/config paths matched at freeze; clean install, installed-tree review, full aggregate validation and full/production audits passed with zero findings. Its exact launcher URL is `http://127.0.0.1:19010`. All 12 CRS recovery cases passed without retries or skips, including catalog A preservation after importing/saving independent B; desktop and phone screenshots were inspected. Full `npm run proof:web` selected all 381 inventory cases with no case filter, one worker and zero retries; its failed/interrupted outcome and followup are recorded below. This web snapshot predates the native-only asset/test-registration repair, not a later application-code change.

The bounded Android attempt failed resource linking because `.gitignore` excluded five existing `splashscreen_logo.png` density variants referenced by tracked XML. All shared resource files in the earlier copy matched; the omission was reproducible from repository inputs, not an Android runtime observation. A narrow exception preserves the existing images unchanged, and `tools/androidResources.test.ts` checks XML reference, density dimensions, decoded nonuniform content and Git-ignore eligibility. The new native snapshot `/home/cyber/cplayout-native-resources-20260914-MF2Wc5/` has 324 matching checked paths and five matching asset hashes. Both root and snapshot resource tests pass; independent read-only QA found no defect in that bounded repair. The assets/test remain untracked until audited publication, so eligibility is not committed inclusion. The pixel helper does not check CRCs or alpha-composited visibility. Original asset-generation provenance is unknown; no new artwork is claimed.

Native attempt logs and structured outcomes remain under `/home/cyber/cplayout-native-proof-20260914-rMTCEq/`. The first offline attempt lacked cached artifacts. The first network attempt was explicitly interrupted for excessive JVM/GC resource use and is not a pass. The bounded retry used one Gradle worker, two JVM processors, a 2 GiB heap and in-process Kotlin; it reached the missing-resource failure in 8m25s. The repaired resource-link attempt uses that bounded profile in a new source snapshot. No AVD was launched or APK installed as part of these build attempts.

The repaired native snapshot passed full `npm run validate`, installed-tree review and audit (zero findings). `:app:processDebugResources` passed all 183 executed tasks in 13m4s. A separate empty API-36/x86_64 Pixel 5 profile, `cplayout_refactor_20260914_mf2wc5`, was created under the private native-proof directory. The existing `cplayout_api36` directory still compared equal to its backup after that creation. Neither profile was launched during preparation; assembly remains a separate gate.

The first complete-inventory browser attempt was stopped after a real failure: 35 passed, one failed, and the remainder were not completed (the JSON reporter groups 345 under skipped, including interruption). Exit 130 is not full-suite acceptance. The failed placement test inspected the generated layer while the captured UI still said `Calculating advisory results...`. Source review confirmed request-keyed async results and persistent error indicators. The existing advisory completion test already uses a bounded 60-second readiness allowance; this followup waits on the same actual state before computed-result assertions, including recomputation after saving zones. No global timeout, responsiveness threshold, confirmation gate or geometry assertion is removed. The first corrected placement path passed; focused family checks and a repeated complete suite remain required. Passing within 60 seconds does not settle acceptable model latency.

The first readiness followup finished with one passed, one failed and seven not run. The generated-zone test asserted derived state during the recalculation triggered by its save. Independent QA identified the missing post-save wait before this outcome arrived. The five-wait followup uses a separate log/result directory and preserves both earlier failures. Its test-source hash is `9a3ed89605d5d43b22786b55e955246fec9f3177f044c7ac200a2b7e52e1eab1`; no application source or exported bundle changed.

The next draft packet has started with two isolated core files, `designDraft.ts` and `designDraft.test.ts`. Fourteen initial focused tests passed independently. Review then reproduced two P2 defects: non-enumerable array serialization hooks could rewrite validated XY, and finite machine inputs could overflow aggregate radii while retaining calculation eligibility. The core worker owns only these two files for corrective tests/implementation; an independent reviewer must recheck the fixes before coordinator registration. There is still no installed storage migration or replacement blank-design UI. These additions are outside the frozen browser/native snapshots above.

Continuation scope remains xhigh with required bounded subagents: a high-effort core worker owns draft validation and its tests; a high-effort read-only reviewer checks the fix; a high-effort read-only explorer investigated avoidable advisory recomputation. A separate high-effort geometry worker now owns only request-local field-plan reuse and its focused tests. The coordinator owns browser readiness, native assembly and integration. No worker may change either active verification copy, launch hardware tools or overlap the coordinator's file writes. Focused tests, aggregate validation, audit, complete browser proof and independent review remain distinct gates.

The performance review identified repeated `evaluateLayout` calls for the same candidate during pool construction, selection and final output. The selected bounded correction reuses results only within one request, keys exact coordinates rather than rounded candidate IDs, and hoists the unchanged prior-coverage intersection per selection round. Acceptance requires complete output parity, unchanged input/resolution/constraints, request isolation, fewer actual evaluations and unchanged synchronous/iterator results. Wall-time observations during concurrent native/browser work are diagnostic, not a speedup benchmark. Cost edits still trigger synchronous panel work, and whole-project request keys still invalidate unrelated work; those need separate calculation-specific immutable inputs and cost reranking tests, not a blanket cache.

The five-wait browser followup completed with three desktop passes, one tablet readiness timeout and five not run. That timeout overlapped native compiler memory pressure; it is retained as failed evidence, not excused into a pass. The complete browser repeat will use the next reviewed source snapshot after native resource control is established. Do not increase responsiveness or readiness thresholds to hide this result.

APK assembly exposed a separate process limit: Gradle `--max-workers=1` and JVM processor limits did not constrain Ninja's C++ concurrency. Fifteen compiler processes were still present at the captured diagnostic, with swap almost full. The coordinator sent SIGINT only to the ownership-verified Ninja process; Gradle reported `BUILD FAILED in 27m25s` (147 executed, 182 up-to-date), and all owned Java/Ninja/compiler processes subsequently stopped. This was an intentional resource interruption, not a product compile pass. A command-scoped, snapshot-restricted init script now supplies CMake compile/link pools of two/one and a 1 GiB metaspace ceiling for the retry. Its generated Ninja rules and actual process counts must be checked. No global Gradle settings, installed tools, user AVD or root application sources were changed by this tuning.

Draft followup independently verified the serialization and aggregate-sum repairs (22 tests), then reproduced nonfinite layout output from finite radii: a `1e155` span led to `NaN` acreage, and a near-maximum coordinate plus `1e308` span created infinite vertices. A further numerical-domain guard is required before integration; input-complete/saveable status remains separate from calculation eligibility. No invented physical maximum machine size may substitute for numerical representability checks.

That numerical guard is now implemented and independently reviewed: 27 focused tests and five positive geometry diagnostics pass, including northern/southern UTM, partial sweep and a synthetic 106 km reach. Computational extents, products, angles and consumer budgets remain separate from physical equipment limits, solver success and field accuracy. The core API and both new regression suites are registered for combined validation; no storage migration or new blank-design UI is implied.

Field-plan reuse initially preserved all serialized fixtures but independent review found signed-zero aliasing in numeric Map keys. A private key now distinguishes both signs on both axes. Four strict sign combinations, the original uncached comparison, all 18 existing plan/standalone digests and yield counts pass. Nine operation profiles retain 370 planner evaluations (879 before) and 3074 total evaluations (3583 before); those counts are not a wall-time speedup claim. Cache lifetime remains request-local and requires stable inputs while suspended; memory growth and broader immutable-input work remain open.

The CMake retry passed APK assembly in 4m34s (42 executed, 326 up-to-date). The generated compile/link pools and observed compiler count confirmed the two/one bound. Init script SHA-256: `beaa6a493b2850eed1944368ba630ba695a8f655654d783fa985865cf718ddae`. The 83,489,261-byte x86_64 debug APK has SHA-256 `f8997e44b6a7e185c470090e94cb07cf1d2d680b91f5196d16945aebae666243`; v2 signature verification passes with the Android Debug certificate. The new API-36/Android-16 emulator `emulator-5570` was empty before installation, and its installed APK hash matches. The original user AVD was not targeted.

All 324 frozen native source paths still matched before runtime collection. This APK has no embedded JavaScript bundle. The separately served 13,072,226-byte Android development bundle has SHA-256 `de52fdac41e85084094fbbfe1df5626f4cd39f3a4fc355f0aa350986f6928175`; that download does not establish consumed-device-bundle identity. Initial Metro host/offline flag conflict, a boot-probe absent-package handling error and an unquoted PATH launcher failure were corrected and retained as preparation failures, not runtime passes.

The source-matched native collector exited 1. Report `native-runtime/android-native-verification-20260914-072834073Z.json` under the private native-proof directory retains `status: fail` and `executionScope: single_process`. Its in-app marker observed schema/PRAGMA 11, migrations 1-11 present, geometry rows and map columns, plus native SQLite save/list/load/settings/delete. The in-memory 24,078-byte ZIP round trip passed with SHA-256 `ccf2174d9585f9e81d61014962f5ed1d448efbfe2e0e80f324a57ba74e007227`. No relaunch, upgrade history, native picker receipt or physical observation follows from those facts. This source snapshot predates the new core draft API and field-plan cache.

The failure screenshot shows a System UI ANR over Expo's first-run developer menu. The coordinator retained it, selected Wait, then dismissed the onboarding/developer menu only on the identified isolated AVD. A subsequent screenshot showed the actual CPLayout File menu and rendered icons, but does not prove every asset or native screen. A diagnostic UIAutomator invocation returned a null root while leaving an older XML file; that diagnostic is not accepted navigation evidence. Independent high-effort review also reproduced false share/picker detection from an ANR/app ZIP labels, unobserved selected-file claims derived from host bytes, mismatched pre/post-tap artifacts, and the fresh catalog's absent ZIP controls. A disjoint high-effort worker owns only collector/helper/tests; failures must remain failures, and file receipt must remain unknown until independently observed.

The new integrated snapshot `/home/cyber/cplayout-integrated-20260914-WgcDsc/` contains 504 tracked/nonignored paths matching the root at freeze, including documentation. Clean `npm ci --ignore-scripts --audit=false`, the existing reviewed UUID helper, installed-tree inspection and combined `npm run validate` passed. Full and production audits each report zero findings. All 504 copied paths still matched their recorded hashes after validation. This application includes the draft foundation, signed-zero-safe request-local reuse and five readiness waits; later host-collector/optimizer work is a separate source identity.

The first web export was explicitly interrupted for excessive parallelism and roughly 4.3 GiB resident memory; its raw exit 0 is not accepted as completion. The installed Expo CLI confirms `--max-workers`; `npm run export:web -- -- --max-workers 2` then passed. Bundle `index-d9d4e67a8b4080cd824da865d25368bd.js` has SHA-256 `48eb64cccba630d01e42f3ba548bf101f2561490c533b5587eb7972be322e698`. The repo launcher serves it at `http://127.0.0.1:19011`. The nine-case readiness family is running with one worker, zero retries and failure-fast; its first two desktop cases passed and the generated-zone screenshot was inspected. That is not full browser acceptance. Use bounded exports for subsequent proof runs on this host.

Native cleanup completed: the identified emulator and Metro processes stopped, its sole `tcp:8082` reverse was removed, and no device remained attached. The original `cplayout_api36` AVD and registration compared equal to their backups. A full-logcat postflight timed out; the bounded 1000-line capture subsequently passed and is retained. The isolated AVD data remain available for another commissioning run. The ADB server remains running with no attached device; historical preview listeners were not touched.

Curated [checkpoint evidence](evidence/full-refactor-20260914/README.md) preserves the nine-plan comparisons, signed-zero operation counts and failed native single-process observations with artifact hashes. Governance validation found only stale generated context maps (55 of 56 hook tests passed); regeneration and a full repeat remain required after source/document inputs settle. A further high-effort worker owns only optimizer/tests to remove layout and boundary calculations duplicated after scoring; no new semantic rule or tolerance is authorized by that performance packet. Independent numerical review is separately checking whether legacy project admission can bypass the new draft's computational limits.

The `WgcDsc` readiness family completed with nine passes, zero skips and zero retries in 19.8 minutes. All three cases passed on desktop, tablet and phone. The source-matched JSON/log identities are in `advisory-readiness.json`; this selected result is not full-inventory acceptance, and the observed latency still needs optimization.

Independent QA accepted the second performance change in `pivotCenterOptimizer.ts`: reuse the scored result instead of repeating layout evaluation and conservative boundary calculation. All 20 retained input/result pairs compare strictly equal, including order and signed zero. Worker operation profiles show layout calls 731 to 371 across 11 optimizer cases and 3074 to 1722 across nine field plans; boundary calls fall 720 to 360 and 2704 to 1352 respectively. Those instrumented counts were reviewed from artifacts, not independently recollected. The registered golden tests preserve output/yield behavior; no wall-clock speedup is claimed.

A separate browser diagnostic reproduced a late Files ZIP picker loading and persisting a project after navigation to the catalog. The coordinator moved independent ZIP reads under the shared latest-request guard with component/project-scoped cancellation ownership. Nine guard tests and mobile typecheck pass. Four new browser scenarios cover valid/malformed late reads, navigation to Dashboard and newest-picker ownership; these await the next exported build. Their waits observe FileReader completion rather than a fixed delay. Independent UI review is still required before acceptance.

The legacy numerical review confirmed that valid stored v1 projects could bypass draft admission: a `1e155` span yielded `NaN` acreage, while finite near-maximum coordinates and a `1e308` span yielded infinite vertices. A bounded high-effort worker is extracting shared calculation-domain checks and a finite-result invariant; the independent GIS reviewer will repeat those exact probes. Legacy parsing, recovery and stored XY must remain unchanged. Saveability, calculation eligibility and field qualification remain different contracts.

Independent runtime-review QA accepted the repaired collector after 21 focused tests and a separate stubbed replay of its preexisting-fixture failure. Expected-package nodes hidden below System UI bars and preexisting fixture breadcrumbs are rejected before actions; disabled/failed taps and stale dumps cannot claim success. The five historical Samsung share/picker profiles match and the recorded ANR rejects. Only the observed historical share profile is supported; generic current-device chooser support remains unverified. Receipts stay empty/zero, and `single_process` cannot become a completed native report. The collector suite is registered in the root test command. No device rerun has occurred after this repair.

The numerical implementation is complete and under independent GIS review. It shares the draft budget, guards project-aware geometry entrypoints after CRS checks, and scans every returned layout metric/polygon/hole/tower/conflict for finiteness. Worker-focused checks reported 27 unchanged draft tests, 14 new core tests, 13 geometry tests and nine CRS tests passing; aggregate acceptance remains separate. Two browser regressions now require overflow examples to open only in recovery and preserve their stored/exported XY and span values.

UI review found that accepted ZIP imports lost save acknowledgement after leaving Files. App now owns generation-scoped import feedback, shares normal-save ordering, and acknowledges revision zero only for that imported generation. A later revision remains dirty; another loaded project cannot inherit the acknowledgement. Eleven import scenarios (33 viewport cases) cover cancellation, malformed files, competing opens, edits, save success, quota failure and retry. These are pending the next frozen browser run. Web export now uses the verified two-worker CLI limit by default so `npm run proof:web` includes the bounded build.

The frozen `F8dZtG` candidate contains 515 matched tracked/nonignored paths. Clean install, installed-tree review, bounded export and both audits passed with zero findings. Its full `npm run validate` failed: the new blanket project guard incorrectly applied clipping/sector limits to the standalone normalized clearance solver. This is a compatibility regression, not just a changed error message. Existing scale-`1e100`, translated and unresolved-precision quality cases must remain supported. All 33 import and 18 CRS/numerical recovery browser cases passed on its exact `http://127.0.0.1:19012` export, without retries or skips; these selected passes do not override the aggregate failure. Phone failure feedback and desktop legacy recovery screenshots were inspected. The candidate and its failed log remain unchanged.

The coordinator corrected only the standalone consumer boundary: retain CRS-first and solver-specific range/options checks, add a finite ordered output-bounds invariant, and remove its unused radius option from shared project admission. Full layout/coverage/project-level callers retain admission and the finite-result invariant. The original quality suite was not edited. The corrected focused run passed 66 test-runner cases, including that quality suite's internal analytic, scaling and oracle assertions. Independent re-review and a new aggregate/browser candidate are required before integration acceptance.

Draft archive acceptance is separately blocked. Independent adversarial review reproduced missing CRC verification that changed XY, lossy invalid UTF-8, unsupported flags/corrupt directory headers, and unchecked descriptor metadata. Nineteen original tests passed but did not catch these defects; four valid controls and 15 malformed accepted fixtures are retained. The archive worker owns only its two files for fixes and regressions. Coordinator export/test registration follows independent acceptance; no UI import, catalog storage or migration is enabled by this unfinished packet.
