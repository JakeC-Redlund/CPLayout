# CPLayout Agent Known Gaps

This file tracks gaps in the agent/skill process layer. It does not replace product validation checklists.

The current [mapping workflow packet](mapping-workflow-review.md) fixes local numerical, scoring, selection, save-status and hook-payload regressions. The [continuing refactor](full-refactor-execution.md) adds strict planar CRS admission, recovery, and pure transport switching guards; full integration/browser acceptance is in progress. True incomplete drafts, atomic catalog creation, grid-to-ground/datum/height qualification, immutable Layout snapshots, continuous corner/linear feasibility and physical RTK proof remain open. Successful local checks do not close these gaps. Web checkbox state required direct ARIA props in the installed renderer; the wider accessibility inventory and native screen-reader parity remain unverified.

The new core draft document is exported/test-registered after focused review, but storage/archive/UI integration is not implemented. Request-local field-plan and optimizer reuse passed strict parity review; cost edits still run synchronous panel calculations and whole-project keys still invalidate unrelated results. Legacy numerical admission and pending ZIP ownership corrections await combined acceptance. The isolated Android build/install and current-process schema-v11 SQLite/in-memory ZIP observations are retained, but the overall collector failed at OS UI navigation. The narrowed OS classifier/collector repair passed 21 focused tests and independent review; it has not been rerun on a device. Restart/upgrade, received-file/consumed-bundle identity and physical evidence remain separate. Nine `WgcDsc` readiness browser cases now pass across desktop/tablet/phone; previous failures remain retained, and complete-inventory acceptance for later source is still required.

## Compact Gap Index

- [Process And Governance Gaps](#process-and-governance-gaps): reasoning routing, subagents, hooks, token budgets, source ledgers, generated context maps.
- [Runtime Proof Boundaries](#runtime-proof-boundaries): Android/iOS, SQLite/ZIP, MapLibre, app-review, visual-proof, and native/runtime non-claims.
- [RTK GNSS Hardware Gaps](#rtk-gnss-hardware-gaps): physical receivers, corrections, antennas, coordinate epochs, field controls, and native transports.
- [Imagery KML And Google Earth Gaps](#imagery-kml-and-google-earth-gaps): KML/KMZ visual metadata, Google Earth render proof, Google Earth-inspired UI, local visual artifacts.
- [Storage Archive And Native Gaps](#storage-archive-and-native-gaps): project archive round trips, web/native repository split, Android schema-v11 proof gap, historical schema-v10 report scope, web SQLite research gate.
- [ML CV And Companion Evidence Gaps](#ml-cv-and-companion-evidence-gaps): imagery evidence packets, local companion ML/CV, pivot-locator fixtures, on-device ML.
- [Design And Advisory Planning Gaps](#design-and-advisory-planning-gaps): corner-arm, LRDU/SDU, CornerGPSMap, generated design reports, sample fixtures, certified-design non-claims.

## Process And Governance Gaps

Use this section for agent routing, managed hooks, generated context maps, route keyword budgets, and source-ledger process caveats.

| Gap | Impact | Current mitigation |
| --- | --- | --- |
| Dynamic reasoning routing is documented and tested, but runtime model settings remain session-selected. | `AGENTS.md`, route data, skills, and custom agents no longer pin a repo-wide xhigh default; the prompt hook can advise a coordinator complexity band, coordinator reasoning effort, and task-selected subagent reasoning requirement. It cannot change an already-running session's model settings or prove a subagent used a particular effort level by itself. | Start each non-trivial pass with an explicit complexity analysis and selected coordinator reasoning effort; use `xhigh` only when warranted by native/runtime proof, architecture, storage, managed policy, release gates, Google Earth proof, or broad cross-module risk. Give every subagent its own bounded scope and task-selected reasoning level. |
| Custom agent runtime spawning was exercised in the 2026-06-02 plan-consolidation pass, but fresh-session hook-injected routing remains unverified after merge. | Subagents returned useful read-only findings in this session, but automatic route-to-agent behavior depends on the active runtime, trusted project config, and available multi-agent tools. | Keep agents read-only by default; in the next fresh session verify prompt-triage context and record either spawned specialists or `Accepted fallback:`. |
| Persistent subagent authorization is now documented, but local hooks cannot guarantee execution by themselves. | The owner preference is durable in `AGENTS.md`, skill references, and prompt registry, yet a coordinator could still ignore advisory context in an unmanaged session. | Use managed `requirements.toml`, managed hooks, and reviewer discipline for stronger process adherence; continue to require explicit subagent decision summaries. |
| Hook scripts are validated directly, but true in-session injection still requires a fresh Codex session after merge. | The `UserPromptSubmit`, `SubagentStart`, and `PreToolUse` scripts return valid sample output where expected, while the disabled Stop compatibility script must return no output. Active injection depends on trusted project config and runtime hook loading. | Start a fresh Codex session on the merged branch and prompt `Use Google Earth imagery and SQLite to improve center pivot UI`; verify injected context mentions only the top ranked specialists and CPLayout boundaries, and verify no Stop continuation prompt appears. |
| Managed hook enforcement is documented but not endpoint-verified here. | The example `requirements.toml` can parse locally, but non-bypass behavior depends on managed requirements deployment, copied scripts under the managed directory, Codex restart, and `/hooks` verification on the target machine. | Use `docs/codex-managed-hook-deployment.md`; after deployment, verify startup config, managed hook trust status, and sample prompts in a fresh Codex session. |
| Managed prompt triage depends on route data availability when scripts run outside the repo. | A managed script copied to an administrator directory can find route data from the current CPLayout checkout or adjacent managed files, but neither path is endpoint-proven. | Copy `.codex/hooks/cplayout_route_data.json` with managed scripts or run Codex from the CPLayout checkout; verify a matched prompt emits expected route ids. |
| Hook guardrails are not full command enforcement. | Local `PreToolUse` can deny narrow destructive commands, but broader policy still depends on model behavior and managed deployment. | Use managed requirements `[rules].prefix_rules` for hard command prompt/forbid policy if stronger enforcement is required. |
| Stop-hook continuation behavior is disabled. | Current official docs expose Stop continuation, but CPLayout observed repeated continuation prompts from stale or ambiguous thread context. `.codex/hooks.json` no longer registers Stop, and `cplayout_stop_multi_agent.py` is a silent compatibility no-op. | Keep missing subagent accounting visible through `AGENTS.md` and `UserPromptSubmit` context only; do not re-enable Stop continuation without a fresh-session payload audit and loop-proof tests. |
| Prompt triage is weighted keyword scoring. | Broad or unusual prompts may miss a relevant specialist or rank specialists imperfectly. | Route data caps output at 3 specialists and keeps broad terms low weight; the coordinator still owns final routing using `AGENTS.md`, repo evidence, and source-backed judgment. |
| Prompt-triage token budgets are enforced locally, not globally. | The hook now trims context-pack summaries to `maxEmittedPackSummaryChars` and validation caps curator keyword growth, but those limits only govern CPLayout repo-local hook output and route data. They do not reduce product runtime cost, prove managed hook loading, or guarantee a coordinator will spawn fewer subagents in an unmanaged session. | Keep route output capped at 3, maintain fixture tests for broad-term and mixed-prompt behavior, use `npm run validate:skills` after route/context changes, and require live restarted-session hook evidence before making managed endpoint claims. |
| Generated context-map refs are advisory and freshness-tested, not enforcement. | `.codex/hooks/cplayout_context_map.json` can point prompt triage and subagent-start hooks toward compact route-specific file refs, validation commands, panel weights, and hard vetoes, but it cannot prove hooks loaded, subagents spawned, reasoning effort changed, or any product/runtime behavior passed. | Keep `npm run context-map:check` inside `npm run validate:skills`, copy the context map beside managed hook scripts when deployed outside the repo, and require live restarted-session hook output before making any managed endpoint claim. |
| New runtime proof, GIS geometry, and QA validation specialists are routing aids, not proof authorities. | `cplayout_runtime_proof_gatekeeper`, `cplayout_gis_geometry_guardian`, and `cplayout_qa_validation_reviewer` improve prompt triage and review boundaries, but their presence cannot prove native/runtime behavior, canonical geometry correctness, or validation acceptance. | Keep them read-only by default, require task-selected subagent scopes, and require the relevant checklist/report/source evidence before accepting any runtime, geometry, or release claim. |
| Route ambiguity remains possible despite narrow phrase weighting. | Strong phrases reduce broad over-routing, but mixed prompts can still rank only three specialists under `maxRoutes: 3`; lower-ranked specialists may be omitted from hook context even when useful. | Keep broad terms below threshold, add fixture tests for new phrase families, and let the coordinator override advisory routes from current repo evidence. |
| Live restarted-session hook verification is still missing for the expanded routes. | Local script tests and context-map checks can prove parse/freshness behavior, but they do not prove a newly launched Codex session trusted and injected the expanded route context. | After merge or handoff, start a fresh session, run sample prompts for runtime proof, GIS geometry, and QA validation, and record whether hook context and subagent-start context appear as expected. |
| Managed endpoint proof remains unverified for the expanded route set. | The managed configuration docs and local example can describe deployment, but no endpoint has shown the new route data, context map, and scripts copied under a managed directory and active after restart. | Deploy through managed requirements only when needed, copy scripts plus route/context data together, verify `/hooks`, and keep claims as advisory until the endpoint evidence is recorded. |
| Context and token-budget drift can recur as specialists and route terms grow. | Three new specialists increase route data, context-map source hashes, skill metadata, and hook summary pressure. A future broad keyword expansion could exceed budgets or hide important first-read context. | Keep `maxRoutes: 3`, context-pack summary caps, route keyword limits, and `npm run validate:skills`; prefer narrow phrase tests over adding general terms. |
| Google Earth-inspired help prompts can be under- or over-routed if phrasing drifts. | Phrases such as companion evidence, map imagery organization, import wizard, and onboarding help need imagery, interface, and curator review, while broad `help` alone should not trigger CPLayout specialists. | Keep phrase-specific route data and tests current; avoid adding broad standalone `help` or `process` weights that would over-route unrelated prompts. |
| Source ledger is a seed, not a complete research knowledge base. | Center-pivot design, UI, database, and imagery methods still need task-specific source review. | Require source-backed updates for each new claim and keep unknowns explicit. |

## RTK GNSS Hardware Gaps

Use this section with `docs/rtk-gnss-integration-plan.md`; source/browser-fixture evidence cannot satisfy these hardware gates.

The current owner-declared board/radio/controller inventory and manufacturer source conflicts are recorded in [receiver hardware qualification](receiver-hardware-qualification.md). The September 13 local/September 14 UTC enumeration established no usable receiver endpoint. Matching EVB assembly, radio firmware, relay thresholds, level-shifting/netlist review and physical 3D controls remain blockers; a software route state machine does not remove them.

| Gap | Impact | Current mitigation |
| --- | --- | --- |
| Physical receiver behavior is unverified. | Strict NMEA tests and Web Serial mocks do not prove a receiver's message mix, firmware semantics, disconnect behavior, or sustained byte flow. | Commission a documented receiver, sanitize replay fixtures, cross-check fixed/float against the receiver, and produce a hashed runtime report. |
| Owner-selected NavSpark receivers need separate profiles. | PX1122R is an onboard RTK solution target; NS-RAW needs a raw-decoder/local-engine path. The PX1122R datasheet does not establish the current generic gate's GST/correction-age message combination on the owner's firmware. | Follow the manufacturer review in the RTK integration plan. Add sanitized model/firmware fixtures and source-backed quality adapters without weakening missing-quality rejection or assuming NS-RAW computes RTK onboard. |
| Correction delivery is unverified. | `writeCorrections` is an interface only; no RTCM stream, NTRIP caster, TLS/authentication, backpressure, or credential-redaction path was exercised. | Prefer receiver-managed corrections first; threat-model and test any in-app client without persisting credentials or raw corrections in projects/reports. |
| Antenna and geodetic metadata are incomplete at capture time. | Capture evidence can represent reference/height/epoch facts, but the browser panel does not yet collect a verified antenna model, ARP height, phase-center model, pole tilt, reference frame, coordinate epoch, or vertical datum. | Add source-backed receiver-session metadata before field proof and require it in the GNSS runtime report. Keep height interpretation explicit and non-certified. |
| Android/iOS transports are missing. | Desktop Web Serial cannot establish tablet/phone receiver support. Android USB needs a native module/config plugin and iOS BLE/MFi has distinct protocol constraints. | Implement Android USB first in a rebuilt Expo development client, then separately prove Android BLE/SPP and iOS BLE/MFi per receiver. |
| Field accuracy and 3D capture eligibility are unverified. | The owner requires maximum error strictly below 0.10 m including elevation. The current capture gate does not require acceptable vertical uncertainty or verified height/antenna reference. RTK-fixed labels and horizontal GST RMS do not prove 3D accuracy. | Add receiver-session vertical eligibility first, then repeatedly occupy independent 3D controls in matching frames. Use physical ENU/ECEF comparison coordinates, not uncorrected grid distances; record all eligible epochs, horizontal/vertical/3D errors, uncertainty budget, ages, antenna facts and hashes. |
| Physical three-way relay-controlled transport is not implemented. | A pure state/effects controller now has 24 synthetic failure/freshness tests; no IO adapter or inspected hardware contract exists. The owner inventory does not establish matching assembly, channel mapping, polarity, safe states or protocol. Neither a software selector nor an ACK proves physical routing. | Specify and bench-check the electrical/control contract before enabling USB/Bluetooth/local Wi-Fi switching. Serialize owned effects, independently verify cleanup/routing and each Android device; iOS remains a later separate compatibility lane. |
| Capture evidence is persisted but not a survey certification. | Project JSON/ZIP round trips preserve provenance, and reducer rules prevent silent observation rewriting, but no qualified survey or machine-control claim follows. | Keep CPLayout planning-only and require qualified review for legal boundary, certified survey, control, or safety use. |
| Evidence validation cannot yet establish physical GNSS accuracy or installed native build identity. | The derived contract validates declared ENU arithmetic, finite heights, metadata and hashes; it does not verify the coordinate transformation or field controls. Native producers record host Git state, not independently linked installed code. | `tools/roadmapCompletion.ts` deliberately blocks these release success paths. Supply independently verifiable transformation/build artifacts and add a separately reviewed verifier; do not bypass the gate with matching strings or booleans. |

## Imagery KML And Google Earth Gaps

Use this section for KML/KMZ visual interchange metadata, Google Earth proof, Google Earth-inspired UI, visual-proof artifact durability, and imagery/ML companion boundaries.

Key anchors:

- `kml-kmz-visual-interchange-metadata`: KML/KMZ styles, labels, LookAt data, screenshots, OCR/CV output, and Google Earth Pro observations are evidence or visual interchange metadata only.
- `google-earth-render-proof-artifact-specific`: Google Earth proof requires non-black rendered map-canvas evidence, overlay confirmation, hashes, attribution/provenance, and uncontaminated cleanup for the targeted artifact/session.
- `google-earth-inspired-ui-not-embedded-runtime`: Google Earth-inspired wording is workflow vocabulary only; it is not embedded Google Earth, Google imagery caching, or native runtime proof.
- `visual-proof-artifacts-local-not-durable`: ignored raw screenshots/reports must be curated with hashes before becoming durable proof records.

## ML CV And Companion Evidence Gaps

Use this section for local companion evidence packets, pivot-center detection, calibrated projected-XY candidate evidence, and on-device ML non-claims.

Key anchors:

- `imagery-ml-companion-evidence-only`: companion packets stay `evidenceOnly: true`, `appImportable: false`, `writesProjectDatabase: false`, and `canonicalGeometryMutation: false` unless a separate projected-XY workflow changes that contract.
- `ml-cv-companion-advisory-review`: image-space and calibrated CV outputs remain advisory evidence until operator review and reducer-backed projected-XY acceptance.
- `native-on-device-ml-unverified`: ONNX/native ML candidates require development-build device proof before any mobile runtime claim.

| The Imagery/ML capability roadmap is active, but most phases are future proof gates rather than shipped behavior. | `docs/imagery-ml-capability-roadmap.md` sequences multi-year work, but it does not by itself create app import, storage, native ML, native imagery, field-trial, or production-readiness claims. | Treat the roadmap as an executive plan; promote a phase only after its listed validation gates, source checks, and proof artifacts pass. |
| `cplayout-imagery-evidence-v2` now has a strict core validator, Python emitter, and standalone verifier, but it is still not a project or app-import schema. | `packages/core/src/imageryEvidence.ts` can classify strict v2 companion packets as `blocked` or `ready_for_read_only_report` and candidate rows as `metadata_only`, `blocked`, or `calibrated_projected_xy`; `build-evidence-packet` now emits the v2 packet shape with local-only flags, visual metrics when a readable local image is supplied, attribution, calibration metadata, and projected truth gating; `npm run verify:imagery-evidence` can validate a packet locally and optionally require a calibrated projected candidate. Teams could still mistake that for permission to persist raw imagery/CV/model evidence in `PivotProject`, project ZIPs, native SQLite, KML/KMZ exports, or browser project storage. | Keep the packet companion-owned with `canonicalGeometryMutation: false`, `evidenceOnly: true`, `appImportable: false`, and `writesProjectDatabase: false`; use the emitter and verifier for read-only review gates only until a separate Phase 4 reducer-confirmed projected-XY draft workflow is implemented and tested. |

## Design And Advisory Planning Gaps

Use this section for irrigation design records, corner-arm/LRDU/SDU evidence, CornerGPSMap/BPF, generated scenarios, local sample fixtures, and certified-design non-claims.

| Guided manual design and edit ergonomics remain native-device unverified. | The session-only guided transaction now stages map-click boundary, pivot, last-wheel, and machine-end inputs without project mutation, then validates and applies the complete draft in one reducer revision. MapLibre and forced SVG browser proof cover reducer-backed segment insertion and direct drag/reposition controls on desktop, tablet, and mobile. | Keep the source/static-browser claim bounded. Run Android/iOS device proof before claiming native MapLibre/SVG gesture parity, touch ergonomics, persistence, or production readiness. |
| Hydraulic and agronomic suitability remain outside geometry readiness. | Readiness reports missing flow, pressure, application intensity, friction loss, nozzle package, topography, and uniformity inputs, but it does not calculate or certify them. | Keep geometry output advisory and require qualified engineering review before final design or equipment/controller decisions. |

| Local design-guide summaries are advisory records, not project data. | The six local Valley design-guide PDFs now have page counts and curated page/line-backed topic summaries in `docs/design-guides/`, but the PDFs, extracted text, manifests, and tool reports remain local research artifacts. They must not become `PivotProject`, native SQLite, web local-storage, ZIP archive, KML/KMZ, or automatic projected-XY geometry data. | Keep raw artifacts under ignored `reports/design-guides/`, commit only curated summaries with source hashes and page/line references, and require `npm run validate:design-guides` when these records change. |
| Local corner service-manual summaries are advisory records, not controller proof. | The local Valley Corner, VFlex Corner, Precision Corner, DualSpan Corner, and Corner Service Manuals Index PDFs now have curated metadata in `docs/corner-service-manuals/`, but the PDFs, extracted text, manifests, and tool reports remain local research artifacts. They must not become proprietary kinematics, controller compatibility, controller payload export, app import, project schema, SQLite/web storage, ZIP archive, KML/KMZ, or automatic projected-XY geometry authority. | Keep raw artifacts under ignored `reports/corner-service-manuals/`, commit only curated summaries with source hashes and page/line references, and require `npm run validate:corner-service-manuals` when these records change. |
| Advisory corner-arm config and envelopes do not prove proprietary kinematics. | Public manufacturer pages and local PDFs can guide metadata and review surfaces, but CPLayout does not yet model manufacturer-specific steering, sequencing, or sprinkler timing accurately enough to merge corner-arm coverage into layout feasibility. | Store `machine.cornerArm` as optional advisory data only, keep `evaluateAdvisoryCornerArm` separate from `evaluateLayout`, and require qualified review plus source-backed tests before treating corner-arm coverage as allowed coverage. |
| Advisory LRDU/SDU drive-unit metadata is not controller proof. | CPLayout can store optional advisory tire labels, custom RPM, and measured-speed metadata through JSON/XML/archive/KML surfaces, but public tire pages do not supply motor RPM presets and local VManual/FLT values require curated page/line/source-hash records before becoming presets. | Keep `machine.driveUnits` advisory-only, preserve projected/local `XY`, show custom/source-required warnings for blank or custom RPM, and avoid claiming SDU/LRDU steering, speed control, controller payload compatibility, or proprietary path reproduction. |
| Corner-arm scaffold artifact lineage is unresolved. | `tmp/Valley Corner Arm Specs_A.xlsx` and the workbook embedded inside `tmp/irrigation_corner_arm_initial_scaffold.zip` have different SHA-256 hashes. The standalone file is a single-sheet scaffold without the normalized source/status contract. | Keep both hashes in `docs/corner-arm-artifacts/`; use normalized scaffold records only as source-tagged advisory metadata; do not promote either workbook to production authority until lineage and provenance are confirmed. |
| Corner-arm path-solver production inputs remain absent. | The scaffold identifies required inputs, but actual machine serial/configuration, pivot-center-to-LRDU radius, measured LRDU speed, selected local projected CRS, field boundary, obstacles/no-go buffers, guidance path, orientation, rotation direction, angle/steering limits, and sampling policy are not supplied. | `evaluateCornerArmKinematics` blocks output when required projected-XY/model/speed/boundary/guidance inputs are missing; existing envelope overlays remain fallback advisory display only. |
| Corner-arm workbook/spec values are not production authority. | Normalized rows are all `production_ready=No`; standalone workbook rows differ from embedded evidence and lack source/status columns. | Store typed scaffold catalog rows with `sourceStatus: scaffold_only` and `productionReady: false`; require owner/dealer/manufacturer confirmation before using values for production design. |
| CornerGPSMap BPF/config ingestion is advisory evidence, not a Valmont controller workflow. | CPLayout now parses CornerGPSMap-style BPF boundary/center files, config-derived model metadata, source-labeled review thresholds, and redacted FLT/CornerGPSMap inventory summaries, but it does not reproduce proprietary `CornerPath`, GGS, VRI, steering, SDU/LRDU control, or certified dual-swing behavior. BPF latitude/longitude remains WGS84 input/display evidence until projected into the project CRS and explicitly applied through the Files workflow. | Keep fixtures synthetic/redacted, import BPF through preview/apply selection only, store canonical geometry as projected/local `XY`, keep BPF center points as survey evidence unless an operator explicitly moves the pivot, keep detailed inventory manifests local unless explicitly selected, and preserve `evaluateCornerGpsMapAdvisoryReview` as advisory clearance review only. |
| Center-pivot awareness features are not certified automated design. | CPLayout can now preserve planning boundaries, machine zones, linear move paths, measurement lines, pump/well locations, underground pipe/wire paths, power poles/lines, existing pivot evidence, minimum-clearance gates, operator-supplied local cost-input status, transient machine-zone candidate reviews, multi-zone scenario compilation, generated within-field multi-pivot advisory screening, generated multi-pivot scenario rows with selected centers, overlap acres, separation margins, separation rejection deficits, and cost-evidence status, SVG/MapLibre generated-plan overlay display, explicit save-to-machine-zone review features, local advisory design report preview/export with current/missing/stale generated review-zone audit, full-scope compiled-boundary geometry and coverage/dry-acre metrics, projected-XY collision/review zone evidence with severity flags, advisory current/full-circle machine strategy comparison, shared advisory radius-sensitivity rows for generated full-circle alternatives, shared advisory sweep-efficiency rows for part-circle versus same/shorter full-circle alternatives, path-based advisory linear/lateral swept-strip scoring, operator-labeled bender/second-pivot opportunity envelopes, and advisory obstacle interaction summaries for hard-blocking, no-spray, span-clearance, tower-track, utility-path, outside-reach, operator/source crossing-profile, blocked-profile, and clearance-shortfall evidence. That does not prove first-class saved multi-pivot machines, runtime multi-pivot collision avoidance, proprietary bender tower-pivot kinematics, certified obstacle crossing/passability, certified linear/lateral design, vendor cost quotes, certified/final client deliverables, or the Will Rhea / Jason Harmelink final layout. | Keep these features advisory and projected-XY only. Require explicit operator apply actions, local cost inputs, qualified field/vendor review, and future focused tests before promoting first-class multi-pivot project schemas, runtime collision prevention, proprietary bender mechanics, certified crossing/passability, certified linear/lateral design, vendor quote workflows, or certified/final client deliverables beyond local advisory report exports. |
| The Will Rhea / Jason Harmelink KMZ, guided demo, and generated advisory report path are evidence records, not a final client design. | `tools/generateClientKmzAdvisoryReport.ts` reads operator-supplied KMZ/KML evidence, hashes it, imports the full-scope boundary, machine zones, LRDU measurement line, pivot evidence, and selected companion circle-outline KMLs into an ephemeral projected-XY analysis project, and writes local ignored report files with `canonicalGeometryMutation: false`, `writesProjectStorage: false`, `googleEarthRenderProof: false`, and `finalClientDesign: false`. The app guided demo summarizes the same current sample evidence and blockers, but the sample still has no verified corner-arm config/model, no measured LRDU speed, no operator-confirmed orientation/rotation, and no projected-XY `linear_move_path` SDU guidance path. `South_Machine_Field_Boundary` and `Middle_Machine_Field_Boundary` are advisory planning/map evidence only; they are not containment constraints, exclusion boundaries, collision blockers, or substitutes for overhead power-line evidence. | Use the KMZ/report/demo path as an input audit, generated-center review, radius-sensitivity comparison, acre-ledger review, separate-evidence status record, and training walkthrough only. Do not add further client artifacts without explicit owner selection, do not synthesize missing corner-arm inputs, and do not promote walkthrough completion to design readiness. Power-line conflicts require separate `power_line`/`power_pole` evidence or a clearly labeled provisional/verified exclusion before any approval-ready claim. Next progress needs operator/qualified review of radius, zones, obstacle/utility evidence, design alternatives, measured speed, source-labeled model/config, guidance path, orientation/rotation, and cost assumptions before any Will Rhea / Jason Harmelink deliverable or demo can be promoted beyond local advisory evidence. |
| MapLibre edit controls remain native-device unverified. | Browser MapLibre now exposes boundary/obstacle/saved-feature vertex selection, previous/next navigation, validated midpoint insertion, direct drag, nudge, valid delete controls, and circle radius-handle resizing through projected-XY reducer callbacks. SVG fallback exposes matching insertion and drag/reposition behavior, but no Android/iOS device interaction was run. | Treat this as static-browser proof only. Use `npm run proof:web` for browser regressions and `docs/android-native-verification.md` before making native app claims. |
| Process records can lag product code after implementation passes. | Earlier process-only updates did not change runtime code, but later passes may add storage, UI, or companion behavior that needs record updates. | Keep this file, the focused plan, and source ledger synchronized after product changes; do not use older process-only statements as current product status. |
| No first-class ML/CV pivot-center locator loop existed before the 2026-06-01 loop document. | Existing browser mapping loop and boundary-focused ML reports cannot prove automatic center-pivot locating quality. | Use `docs/ml-cv-pivot-locating-improvement-loop.md` and `npm run verify:ml-cv-loop` as the pivot-locator control loop until executed evidence rows replace planned rows. |
| Local companion pivot detection and curated real-fixture manifest ingestion exist, but broad real-world projected-XY pivot quality remains unproved. | Image-space Hough circle detections may identify crop rings or overlays, and the public Adams County fixture proves one calibrated companion report path. That single curated fixture is not enough to claim general automatic pivot locating. | Use `detect-pivot-candidates` for advisory image evidence; use `npm run real-pivot-fixture:generate` and `cplayout-real-pivot-fixtures-v1` only with operator-approved truth, project-CRS calibration, matching hashes, and no canonical geometry mutation. |
| Pivot-locator fixture evidence is not yet curated as durable checked-in summaries. | Ignored local reports may be useful but cannot serve as durable project knowledge or regression proof. | Create milestone summaries under `docs/evidence/continuous-improvement/ml-cv-pivot-locating-*` with artifact hashes and validation results. |
| Google Earth render proof is artifact-specific. | The strict local packet at `reports/google-earth-visual-fidelity/20260604T-render-proof-kml-strict/visual-fidelity-manifest.json` records passed Google Earth Pro evidence for that KML/KMZ artifact: `proofPassed: true`, overlay confirmation, non-black map-canvas metrics, and uncontaminated cleanup. That proof does not transfer to future exports or native/mobile behavior. | Require non-black rendered evidence, visible overlay confirmation, artifact hashes, and uncontaminated cleanup status for each new Google Earth visual-fidelity claim. |
| Google Earth-inspired UI wording could be misread as embedded Google Earth or Google imagery use. | The Help route, Places/Layers grouping, and import wizard vocabulary mirror familiar desktop concepts only; they do not grant imagery rights, cache Google imagery, or run Google Earth inside CPLayout. | Use “Google Earth-inspired companion workflow” wording, keep Google Earth Pro as local companion evidence only, and keep screenshots/styles/LookAt/labels out of canonical projected `XY`. |
| Command-surface parity can be mistaken for companion/app workflow parity. | A menu item can route to Files, Settings, Help, or local reports, but this does not make local ML/CV companion output app-importable or apply-ready. | Keep top menus UI-only, preserve the bottom HUD for drawing commands, and mark companion outputs `evidenceOnly: true`, `appImportable: false`, and `writesProjectDatabase: false`. |
| Curated sample fixtures improve demo coverage but remain narrow. | The full-circle, partial-sweep, end-gun, corner-arm, and synthetic full-scope multi-pivot cost demo samples prove fixture plumbing, advisory scoring, sweep-efficiency rows, end-gun throw sensitivity rows, local cost-per-acre comparison, report export, and projected-XY UI display over bounded synthetic/local project shapes; they do not prove broad real-world pivot-layout quality, certified end-gun/nozzle/hydraulic design, certified cost estimates, or the Will Rhea / Jason Harmelink final layout. | Use them as regression fixtures and app samples only; broaden real-world proof through operator-qualified fixtures with source hashes, project-CRS calibration, qualified cost/vendor/nozzle/hydraulic review, and rejection audits. |
| Generated visual-proof reports can be locally useful but are not durable records by default. | Raw screenshots and window XML under `reports/visual-proof/` are ignored and may be absent in a fresh checkout, so claims depending on them can become stale or unverifiable. | Check in curated summaries and hashes under `docs/evidence/...` when a proof claim must survive cleanup; rerun proof commands when raw artifacts are missing. |

## Storage Archive And Native Gaps

Use this section for project repository split, archive round-trip validation, Android report scope, web SQLite, native SQLite/ZIP, and raw tile package rendering boundaries.

Key anchors:

- `storage-archive-native-proof-boundaries`: storage/native claims require the relevant project-store, archive, and platform report evidence.
- `web-vs-native-project-repository-split`: web MVP persistence and native Expo SQLite remain separate repository adapters.
- `archive-round-trip-validation`: browser/local archive round trips prove serialization/import through `projectArchive.ts` and `projectDocument.ts`, not OS share/picker behavior.
- `android-schema-v10-historical-artifact-specific`: Android schema-v10 SQLite/ZIP proof applies only to the completed 2026-06-05 SM-P613 report scope.
- `android-schema-v11-runtime-proof-missing`: current Android SQLite/ZIP proof requires a completed schema-v11 report; source/tooling targets v11 but no completed v11 device report is cited here.
- `native-runtime-non-proof`: compile, layout, and app-review evidence do not prove feature-specific native persistence or MapLibre behavior.
- `web-sqlite-research-gate`: web SQLite remains behind Expo SQLite WASM plus COOP/COEP deployment and proof.

## Runtime Proof Boundaries

Use this section for runtime checklist boundaries that cross storage, map rendering, Android/iOS, Google Earth, browser proof, and ignored report artifacts.

| Project ZIP canonical package behavior is browser/local-proven, legacy review files are ignored, and Android ZIP share/picker regression proof is missing for current schema v11. | Project-store and Playwright prove canonical archive parsing/export/import for web local storage, and the v10 SQLite migration drops retired review tables. `packages/project-store/src/projectRepository.test.ts` now includes a bounded synthetic large-project stress fixture with 96 boundary vertices, 24 obstacles, 93 survey points, 92 map features, and 31 catalog projects, proving local repository save/load plus archive round trip without leaking client profile text into `project.json` or Google Earth KML. `reports/android-native-verification/android-native-verification-20260605-134229571Z.json` validates on Samsung SM-P613 for schema v10, native share-sheet ZIP export, Android DocumentsUI ZIP import, and manifest/project id/CRS agreement, but that report is historical artifact-specific after schema v11. This does not prove current Android schema-v11 behavior, iOS behavior, native large-project behavior, broad performance budgets, or every future archive stress case. | Use `docs/android-native-verification.md`, `npm run verify:android-native -- --report <completed-v11-report.json>`, and newer completed platform reports before making new Android, iOS, native-scale, or larger performance claims. |
| Roadmap completion has profile-aware automation, but physical/runtime resources still require completed reports. | Fast/checkpoint profiles expose missing work; dry runs are side-effect free; release requires a clean tree and current-commit Android/GNSS/Google Earth/MapLibre evidence. The runner cannot enumerate invisible hardware or fabricate field proof. | Use `npm run verify:roadmap` for checkpoints and `npm run verify:release` for release evidence. Supply the feature-specific reports through documented CLI flags/environment variables. |
| Expo Doctor reports a native-folder/app-config synchronization warning. | The repository contains generated `android/` and `ios/` projects while `apps/mobile/app.json` also owns prebuild configuration. Expo reports that orientation, appearance, platform configuration, and plugins will not synchronize unless the build process regenerates or explicitly maintains those native projects. | Before a native release, choose and document one native-project lifecycle, regenerate/reconcile with the pinned Expo SDK as appropriate, inspect the native diff, rebuild the development client, and repeat feature-specific device reports. |
| Dependency security is patched; complete release validation remains open. | The five high Expo/Metro findings reported on 2026-09-13 were removed by the reviewed coherent patch packet. Full and production audits in the 2026-09-14 `WgcDsc` clean-install snapshot report zero findings. The earlier wrapper/dedupe experiment remains rejected. An audit is time-specific and does not prove runtime compatibility or overall security. | Keep exact inventory, rollback and install evidence in `docs/dependency-upgrade-plan.md`; repeat audits and full source/browser/native gates for subsequent candidates. No forced downgrade or major repair was used. |
| Android device transport is host-state dependent on this Windows/WSL setup. | A connected Samsung SM-P613 can be exposed to WSL through `usbipd bind --busid 12-1`, `usbipd attach --wsl --busid 12-1`, a Samsung ADB udev rule, and `adb devices -l`; after WSL crashes or USB reconnects, ADB can fall back to no-device, no-permission, or unauthorized states. This transport was sufficient for the 2026-06-03 Android runtime report, but it remains a host-state dependency for future runs. | Re-run `npm run check:android-tools` first. If WSL loses the tablet, reattach bus `12-1` with USBIPD, ensure `/etc/udev/rules.d/51-android-samsung.rules` covers vendor `04e8`, restart ADB, and require `device` state before native proof commands. |
| Native iOS persistence, current Android schema-v11 SQLite/ZIP sharing, iOS ZIP sharing, raw PMTiles/MBTiles native rendering, Android imported raster/aerial package rendering, and broad native MapLibre parity remain unverified here. | The agent process must not report production-ready native behavior from compile or config checks alone. Android SQLite plus ZIP share/picker has historical SM-P613 schema-v10 artifact-specific evidence. The 2026-06-05 Android vector TileJSON/template MapLibre report records positive tile requests for that historical run; it does not verify the current source, dependency graph or installed build. Raw local tile archives, imported raster/aerial packages, current Android schema-v11 SQLite/ZIP, iOS, and broader production map parity still need their own proof. | Use `docs/android-native-verification.md`, native MapLibre reports with verifiable build identity, and platform-specific checklists before making runtime claims. |
| Offline NAIP-style aerial packages are implemented as metadata/import contracts, but Android local raster package rendering is not yet runtime-proven. | Core, archive, SQLite, and UI code can resolve generated raster TileJSON/templates, but there is no completed SM-P613 development-client report showing an imported package rendering through MapLibre RN `RasterSource` from app document storage. | Use the Android Free Aerial Imagery Package Proof checklist in `docs/android-native-verification.md`, capture screenshots and hashes, keep attribution visible, and prove vertices are unchanged before reporting production Android aerial package behavior. |
| Android tablet console layout is web-proven but not Android runtime-proven in this pass. | Playwright tablet portrait and landscape projects can prove no page scroll, visible drawer handles, and bottom HUD placement in the browser export. That does not prove Samsung SM-P613 native safe-area or Android system-navigation bounds. | Complete the Android tablet console checklist in `docs/android-native-verification.md` before reporting native tablet layout proof. |
| Android app-review harness evidence is advisory and narrower than feature-specific runtime proof. | `npm run review:android-app` can capture Android screenshots, UIAutomator XML, OCR/CV metrics, logcat, findings, and weighted panel decisions across normal routes. It still does not prove SQLite/ZIP, Android share-sheet/DocumentsUI processing, native MapLibre tile rendering, raw PMTiles/MBTiles, iOS behavior, or projected-XY geometry correctness by itself; those claims come from their dedicated reports. | Treat app-review reports as route/layout evidence. Use `npm run verify:android-native -- --report <completed-report.json>` for native SQLite/ZIP, `npm run verify:native-maplibre` for MapLibre tile rendering, and curate hashes under `docs/evidence/` before making durable proof claims. |
| Native/on-device ML for pivot locating remains unverified. | ONNX Runtime React Native and mobile deployment remain candidate technology only; Expo Go cannot prove arbitrary native-library behavior. | Keep ML/CV pivot locating in the local companion until an Expo development build proves model load, inference, latency, memory, power, network isolation, and parity on Android and iOS. |
| Whole-codebase 100-iteration loop includes blocked evidence outcomes. | `docs/whole-codebase-improvement-loop-2026-06-01.md` is batch-classified through row 100, but rows marked `Blocked` are unresolved proof or implementation gaps rather than pass claims. | Use `npm run verify:whole-loop` to enforce row continuity and evidence requirements; do not promote blocked rows until their own proof gates pass. |
| Whole-codebase loop verification cannot prove native, Google Earth, real-world ML/CV, or browser runtime behavior by itself. | The verifier checks ledger structure and passed-row evidence fields, not external devices, visual render state, or real-world fixture truth. | Keep each proof class behind its own validation gate and checklist. |
| Whole-codebase loop rows 011-100 are batch-classified rather than individually hand-implemented feature changes. | The loop records research, classification, validation, and blockers across the codebase, but some rows deliberately stop at documented evidence or blocker inventory. | Treat batch evidence as risk inventory; use each blocked row as the next targeted implementation slice before claiming product behavior. |
